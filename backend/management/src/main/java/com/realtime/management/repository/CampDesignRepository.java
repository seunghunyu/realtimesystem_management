package com.realtime.management.repository;

import com.realtime.management.entity.CampDesign;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CampDesignRepository extends JpaRepository<CampDesign, String> {
}
